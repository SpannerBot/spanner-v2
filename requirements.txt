py-cord==2.1.3
orjson==3.7.12; sys.platform != "win32" and "64" in platform.machine
# ^ We don't install orjson on Windows due to a lack of a native C compiler.
# OrJSON is also 64bit only.
python-dotenv==0.20.0
rich==12.5.1
orm[sqlite]==0.3.1
httpx==0.23.0

# Don't install psutil on Windows
psutil==5.9.2

jishkucord==2.5.2
greenlet==1.1.2
aiofiles==0.8.0
beautifulsoup4==4.11.1
setproctitle==1.3.2
validators==0.20.0
cronitor==4.6.0
fastapi~=0.83
uvicorn[standard]~=0.18
humanize==4.3.0
